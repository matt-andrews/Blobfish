services:
  blobfish:
    build: .
    ports:
      - "31415:8080"
    volumes:
      - blobfish-data:/data/blobfish
      - ./docker.toml:/etc/blobfish/config.toml:ro
    environment:
      - RUST_LOG=info

volumes:
  blobfish-data: